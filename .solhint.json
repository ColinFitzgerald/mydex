{
  "extends": "solhint:recommended",
  "plugins": ["prettier"],
  "rules": {
    "avoid-throw": "off",
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "func-visibility": ["warn",{"ignoreConstructors":true}],
    "compiler-version": ["error", "^0.8.0"]
  }
}